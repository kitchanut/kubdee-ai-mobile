/**
 * SUPERSEDED (v0.3.14): the problem-report description is now typed directly on the Shopee
 * overlay panel (see showAutomationReportPanel in plugins/templates/AutomationOverlay.kt), so the
 * in-app modal is no longer mounted. flushShopeeScrapeDiagnostic sends finalized reports silently.
 * This file is kept only pending confirmation to delete it.
 */
export default function ShopeeReportModal(): null {
  return null;
}
